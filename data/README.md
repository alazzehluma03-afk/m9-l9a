# Data directory

The recipes TTL fixture lives at `../recipes_partial.ttl` (repo root after the
template is accepted). This directory is reserved for any additional fixtures
you choose to add in Challenge Tier 2 or Tier 3.
